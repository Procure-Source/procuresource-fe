{
  "name": "procuresource-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Dormant ProcureSource backend surface for launch infrastructure checks.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js"
  }
}
