# ProcureSource frontend environment template
#
# Copy this file to .env.local for local development if needed.
# This is a static/public frontend. Do not add secrets here.
#
# NEXT_PUBLIC_* values are exposed to the browser and must be public-safe.

NEXT_PUBLIC_SITE_URL=http://localhost:3000
